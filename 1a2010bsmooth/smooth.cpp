/*input
*/

#include <vector>
#include <cstdlib>
#include <cstdio>
#include <cstring>
#include <iostream>
#include <algorithm>
#include <functional>
#include <sstream>
#include <map>
#include <cmath>
#include <iomanip>
#include <ctime>
#include <climits>
#include <cfloat>
#include <queue>

#define DIST(a,b) abs(a-b)

using namespace std; 

int main(int argc, char const *argv[])
{	
	// freopen("B-small-practice.in","r",stdin);
    // freopen("output.out","w",stdout);


    int testCaseCount;

    cin>>testCaseCount;

	for (int i = 0; i < testCaseCount; ++i)
    {
        int delCost, insCost, min, pixelCount;
        cin>>delCost>>insCost>>min>>pixelCount;

        for (int j = 0; j < pixelCount; ++j)
        {
            
        }

    	cout<<"Case #"<<i+1<<": ";

    	cout<<endl;
    }

}